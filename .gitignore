# ---------------------------------------------------------------------------
# Gradle
# ---------------------------------------------------------------------------
.gradle/
build/
app/build/
!gradle/wrapper/gradle-wrapper.jar

# ---------------------------------------------------------------------------
# 本机环境配置（含本机 SDK 绝对路径，绝对不要提交）
# ---------------------------------------------------------------------------
local.properties

# ---------------------------------------------------------------------------
# 构建日志 / 崩溃日志
# ---------------------------------------------------------------------------
*.log
*.err.log
hs_err_pid*
replay_pid*

# ---------------------------------------------------------------------------
# IDE
# ---------------------------------------------------------------------------
.idea/
*.iml
*.iws
*.ipr
out/
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# ---------------------------------------------------------------------------
# AI 助手工作区（含本机路径与个人笔记，不提交）
# ---------------------------------------------------------------------------
.codebuddy/

# ---------------------------------------------------------------------------
# 参考素材：原 APK、样例电子书等，不属于本工程产物
# 注意：必须写成 `/data/`（只忽略工程根的 data/）。
# 早期写成 `data/` 会把源码目录 `app/src/main/java/com/lreader/data/` 一起忽略，
# 导致 BookRepository / SettingsStore / VocabRepository 等 5 个核心文件从未进入仓库。
# ---------------------------------------------------------------------------
/data/
*.apk
*.aab
*.ap_
*.dex
*.class

# ---------------------------------------------------------------------------
# 签名文件与密钥（绝不提交）
# ---------------------------------------------------------------------------
*.jks
*.keystore
keystore.properties
signing.properties

# ---------------------------------------------------------------------------
# 用户敏感数据（翻译平台 Key 等）
# ---------------------------------------------------------------------------
translate_keys.json
*.secret
.env

# ---------------------------------------------------------------------------
# 大体积词典源（如需版本管理请改用 Git LFS）
# ---------------------------------------------------------------------------
*.mdx
*.mdd

# ---------------------------------------------------------------------------
# 词典发布资源：不随代码提交（115MB 超过 GitHub 单文件 100MB 限制）
# 上传到 GitHub Release 附件供 App 按需下载，见 README §7.6
# ---------------------------------------------------------------------------
dict-assets/
